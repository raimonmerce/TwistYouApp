import back from './svg/back.svg';
import close from './svg/close.svg';
import settings from './svg/settings.svg';

export const assets = {
    svg: {
        back: back,
        close: close,
        settings: settings
    }
}